    <img class="cover" src="/assets/img/bookcover/<%= data.cover %>">
    <div>
        <p class="title"><%= data.name %></p>
        <% if (data.progress !== undefined) { %>
            <p class="author"><%= data.author %></p>
        <% } else {%>
            <p class="author"><%= data.description %></p>
        <% } %>
        </div>

    <% if (data.progress !== undefined) { %>
    <div class="piebackground home" style="--p:100"> <%= Math.round(data.progress * 1000) / 10 %> %</div>

    <div class="pie home" style="--p:<%= Math.round(data.progress * 1000) / 10 %>"> </div>    

    <% } %>